let namee = "shanu ";
let last = "for vacation";

console.log(namee + last); //this is not the right way

console.log(`hello come here ${namee} we will go outside ${last}`); //this is the optimal way

const newName = new String("shara-ath");
console.log(newName.length)
console.log(newName.toUpperCase());

const newstring = newName.substring(0,4);
console.log(newstring);
const newLine = newName.slice(0,2);
console.log(newLine);